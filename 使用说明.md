# 亚马逊 ASIN 收集器 + 图片下载 v1.1.0

## 从 1.0.0 更新并保留 ASIN

1. 可先在旧插件弹窗中复制 ASIN 留作备份。
2. 解压新版 ZIP，将 `amazon-asin-collector` 文件夹中的全部文件覆盖到原来加载的插件文件夹中。保持原文件夹路径。
3. 打开 `chrome://extensions`，找到原插件，点击「重新加载」按钮。如 Chrome 要求确认新增的图片域名权限，按提示确认。
4. 刷新亚马逊网页，再打开插件弹窗。

**请在原插件上更新，不要卸载后重新安装。** 继续使用原来的 `asinCollectorState` 存储键，保留既有 ASIN 和收集开关；新增图片下载开关默认关闭。

如果首次安装：解压 ZIP，在 `chrome://extensions` 开启「开发者模式」，点击「加载已解压的扩展程序」，选择包含 `manifest.json` 的文件夹，固定插件图标并刷新亚马逊网页。

## 两个独立开关

- **ASIN 收集**：在搜索结果、BS/NS 榜单、详情主商品和可识别的推荐卡片上方显示「收集／取消收集」及「已收集 (数量)」。数量为全球共用列表去重后的总数，跨页面与标签页实时同步。保留「已收集」按钮用于查看数量，点击它不再打开网页小面板；完整列表请点击浏览器工具栏的插件图标查看。
- **图片下载**：在商品详情页右侧显示两个下载操作及下载进度，与 ASIN 收集开关互不影响。

ASIN 列表在插件弹窗中每行显示一个，只读，支持「一键复制」和「一键去重」。关闭功能不删除数据。使用当前 Chrome 配置的 `chrome.storage.local` 保存，关闭浏览器后仍保留；不跨电脑或浏览器配置自动同步。卸载插件会删除插件数据。

## 图片下载：沿用上传的油猴脚本规则

开启「图片下载」后，在详情页右侧提供：

1. **下载主图＋A+**：下载当前主图图库和 A+ 正文图片，生成 `ASIN_amazon_detail_images.zip`。
2. **刷新后仅下载 A+**：刷新当前网页，等待 2.5 秒后按原脚本规则抓取 A+，生成 `ASIN_amazon_aplus_only.zip`。页面加载慢时，可能仍有未加载的图片，可等待内容加载后重试。

沿用原脚本的主图和 A+ 选择器、图片 ID 去重和候选评分、视频及无效图片过滤。A+ 仅包含 `aplus-media-library-service-media` 图片，默认排除品牌故事和对比表。

主图按原来的 `_AC_SX`／`_AC_SY` 规则尝试 1600 尺寸 URL，然后按原顺序尝试原图和展示图；这并不保证源图片实际有 1600 像素。A+ 先尝试展示 URL，再尝试原图 URL。每次下载最多 3 个并发请求，每个请求超时 7 秒，失败后尝试后备 URL。

ZIP 中保留 `main_01.*`、`aplus_01.*` 等命名及 `queue.json`、`manifest.json`，记录候选链接与成功／失败结果。单张失败不会取消其他图片；全部失败时也保留诊断清单。ZIP Worker 不可用时按原脚本降级到页面内打包。

原油猴的 `GM_xmlhttpRequest` 已替换为扩展后台请求，并增加对应图片 CDN 权限。图片请求不携带登录凭据；仅允许亚马逊图片 CDN 的图片路径。不会将图片或 ASIN 上传到其他服务。下载期间请保持当前商品页面打开；关闭图片功能会移除其按钮，当前任务会在检查到关闭状态时停止，不再输出 ZIP。

建议停用原来的同功能油猴脚本，避免同时出现两套下载按钮或重复下载。

## 商城和页面适配

沿用 1.0.0 的 24 个 HTTPS 亚马逊域名及子域名配置，覆盖美、加、墨、巴、英、德、法、意、西、荷、比、爱、波、瑞、日、印、澳、新、阿联酋、沙特、土耳其、埃及、南非及 `amazon.cn`。图片下载扩展到这些匹配的商城页面，识别规则仍与原油猴一致。

Chrome 不支持 `amazon.*` 形式的顶级域名匹配；将来新增商城时需同时更新 `manifest.json` 的匹配列表和 `common.js` 的域名列表。

## 验证范围

已在 DOM 模拟环境中对比原油猴的图片筛选结果和下载 URL 顺序，验证独立开关、旧数据保留、实时计数、动态卡片、并发写入、后台图片二进制传输、请求失败后备、两种 ZIP、刷新后 A+ 标记和打包降级。生成的 ZIP 已通过 CRC 与逐图片字节校验。

当前环境未完成真实 Chrome、实际 CDN 请求与各商城实时页面实测。原脚本已加载范围以外、A/B 测试新布局或 iframe 内的内容可能需要进一步适配。实际浏览器重启持久化也未在本环境实测。

## 文件与权限

- `manifest.json`：Manifest V3 配置。
- `background.js`、`common.js`：存储与共享工具。
- `content.js`：ASIN 商品卡控件和实时计数。
- `popup.*`：两个开关和 ASIN 列表。
- `image-downloader.js`：基于用户上传的 `Amazon Detail Image Downloader 2026-08-11.2` 迁移。
- `image-fetch.js`：替换油猴跨域请求的后台图片接口。
- 权限：`storage`、`clipboardWrite`；图片主机权限为 `m.media-amazon.com` 和 `*.ssl-images-amazon.com`。

开发参考：[Chrome 跨域请求](https://developer.chrome.com/docs/extensions/develop/concepts/network-requests)、[消息传递](https://developer.chrome.com/docs/extensions/develop/concepts/messaging)。
