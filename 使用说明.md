# 亚马逊 ASIN 收集器＋图片下载 v1.2.0

## 更新并保留数据

1. 解压 ZIP，将其中 `amazon-asin-collector` 文件夹内的文件覆盖到原来加载的插件文件夹，保持路径不变。
2. 打开 `chrome://extensions`，在原插件上点击「重新加载」。手机端功能新增了权限，按 Chrome 的提示确认。
3. 刷新已打开的亚马逊页面。

不要卸载原插件。旧 ASIN 和开关继续保留。初次安装则在扩展管理页开启开发者模式，选择「加载已解压的扩展程序」，加载包含 manifest.json 的文件夹。

## ASIN 收集

开启后，商品卡显示「收集／取消收集」和「已收集 (总数)」。计数按钮保留，但点击不打开网页小面板。点击工具栏插件图标查看完整列表。

弹窗支持「一键复制」「一键去重」「一键清除」。清除立即删除当前列表、同步所有页面计数，但不关闭功能。没有撤销，需要备份时先复制。数据保存在当前 Chrome 配置的扩展本地存储，不跨电脑自动同步。

## 图片下载

独立开启「图片下载」后，详情页右侧提供：

- **下载主图＋A+**：先等待扫描、补读当前商品高清图库，再下载两类图片。
- **切换手机端刷新下载A+**：当前标签页临时使用手机请求头并跳转手机商品地址，刷新后自动扫描和下载 A+。

扫描最多约 8 秒，会定位 A+ 以触发懒加载；未手动操作页面时，结束后恢复原滚动位置。不是一次点击就自动切完所有轮播，尚未加载或特殊布局的内容仍可能遗漏。

手机功能不是完整设备仿真，不保证手机页面比桌面图片更多。移动请求规则会在任务结束、关闭图片开关、关闭标签页或三分钟到期时移除；当前已经加载的页面布局不会因此自动改回桌面。需要桌面布局时重新打开普通商品链接。

下载过程显示主图／A+ 数量、成功和失败。若缺少一类，会明确提示，不再把仅下载 A+ 显示成完整主图＋A+ 成功。

ZIP 保留 main_01.*、aplus_01.*，以及 queue.json、manifest.json、diagnostics.json。遇到漏图可提供三个 JSON，便于查原因。20 秒单次超时，网络／超时／5xx 会额外重试一次；每页最多三并发。

建议关闭原来同功能的油猴脚本，避免重复按钮。下载期间保持当前商品页面打开。

## 本次验证

对用户指定商品 B0CMHT536M 的实际桌面 HTML，新版识别 7 张高清主图和 8 张 A+；实际请求这 15 张图片均成功，主图长边均为 1500px。真实图片数据生成的 ZIP 通过 CRC 和逐字节验证。

手机请求实际取得页面，初始 HTML 识别 7 张主图、3 张 A+；这不代表所有用户的最终懒加载结果。浏览器直接访问受限，尚未完成安装本扩展后的真实 Chrome 全链路点击测试。

具体实现与验证边界见同目录《项目总结.md》。
